// console
 /**
  * log : alias de console.log()
  * @param {*} elt element à exploiter
  * @returns
  */
  export let log = (elt) => console.log(elt)

  /**
   * dir : alias de console.dir()
   * @param {*} elt element à exploiter
   * @returns
   */
   export let dir = (elt) => console.dir(elt)

  /**
   * error : alias de console.error()
   * @param {*} elt
   * @returns
   */
   export let error = (elt) => console.error(elt)

  /**
   * debug : alias de console.debug()
   * @param {*} elt
   * @returns
   */
   export let debug = (elt) => console.debug(elt)

  /**
   * info : alias de console.info()
   * @param {*} elt
   * @returns
   */
   export let info = (elt) => console.info(elt)

  /**
   * clear : alias de console.clear()
   * @returns
   */
   export let clear = () => console.clear()

  /**
   * trace : alias de console.trace()
   * @returns
   */
   export let trace = () => console.trace()

  /**
   * warn : alias de console.warn()
   * @param {*} elt
   * @returns
   */
   export let warn = (elt) => console.warn(elt)